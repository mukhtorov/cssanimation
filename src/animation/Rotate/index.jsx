import React from 'react'
import { Container } from './style'
 const Rotate = (props) => {
    return (
        <Container>
            {props.children}
        </Container>
    )
}


export default Rotate